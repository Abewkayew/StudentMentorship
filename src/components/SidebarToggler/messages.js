import { defineMessages } from 'react-intl';

export const scope = 'pharma.student_management.sidebar';

export default defineMessages({
  show: {
    id: `${scope}.show`,
    defaultMessage: 'Show Sidebar',
  },
  hide: {
    id: `${scope}.hide`,
    defaultMessage: 'Hide Sidebar',
  },
});
