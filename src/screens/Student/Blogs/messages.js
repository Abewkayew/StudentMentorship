import { defineMessages } from 'react-intl';

export const scope = 'pharma.student_management.student';

export default defineMessages({
  blogs: {
    id: `${scope}.blogs`,
    defaultMessage: 'Blogs',
  },
});
