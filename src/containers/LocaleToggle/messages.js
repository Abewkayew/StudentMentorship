import { defineMessages } from 'react-intl';

export const scope = 'pharma.student_management.locale_toggle';

export default defineMessages({
  en: {
    id: `${scope}.en`,
    defaultMessage: 'en',
  },
  de: {
    id: `${scope}.de`,
    defaultMessage: 'አማ',
  },
});
